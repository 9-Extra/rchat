[project]
name = "airp"
version = "0.1.0"
description = "简化版 SillyTavern：AIRP 后端"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx",
    "pyyaml",
    "openai>=3.0.0",
]

[[tool.uv.index]]
url = "https://pypi.org/simple"
default = true
